V2 — after hardware:

Reclassification broadcast — when you tap Enemy/Friend, send SNS packet with d.action: 'RECLASSIFY' over the mesh so teammates update

Scrollable entity list — full-screen list view of all friends/unknown/enemies, sortable and searchable

Web Push notifications — background alerts when app is closed

Alarm PIN setup — 4-digit code required to cancel the alarm

Contact preferences — SMS, Signal, email for non-app alerts

Silent Ban enforcement — reject banned GUIDs on login

GUID entry flow — where the user types the GUID they received from admin

Screenshot: cover sheet for the app — remember when done